// Verifica as informações do email temporário.
// Essa função é renderizada na tela se necessário (quando habilitada).
import React from "react";

export const EmailInfo = ({ sessionInfo }) => {
  return (
    <div>
      {/* <h3>ID da Sessão:</h3>
      <p>{sessionInfo.id}</p>s
      <h3>Expira em:</h3>
      <p>{sessionInfo.expiresAt}</p>
      <h3>Endereço de E-mail Temporário:</h3>
      <p>{sessionInfo.email}</p> */}
    </div>
  );
};
